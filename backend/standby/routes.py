"""
Standby Support System API Routes (disabled).

This file is kept only as a placeholder to show that the standby
feature was intentionally removed. All routes and logic have been
deleted, and the router is no longer included in the main app.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/standby", tags=["Standby Support (disabled)"])

